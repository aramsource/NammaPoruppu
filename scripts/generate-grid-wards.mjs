#!/usr/bin/env node
/**
 * @deprecated Use scripts/import-opencity-ward-kml.mjs for real municipal boundaries.
 * Grid placeholders are not used — cities are only added when official ward data exists.
 */
console.error("Use: node scripts/import-opencity-ward-kml.mjs");
process.exit(1);
